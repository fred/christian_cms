<h2> Edit Settings </h2>

<%= error_messages_for :setting %>

<% form_for @setting, :html => { :method => :put } do |f| %>
	
	<%= render :partial => "form" %>
	<p> <%= submit_tag "Save" %> </p>
<% end %>

<%= link_to "Back", settings_path %>
