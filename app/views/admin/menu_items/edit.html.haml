%h2 
  Editando Menu
  %i 
    %u
      = @menu_item.title

- form_for(@menu_item, :url => { :action => "update" } ) do |f| 
  = f.error_messages
  = render :partial => 'form', :locals => { :f => f, :menu_item => @menu_item }
  = f.submit 'Salvar'
  
= link_to 'Volver', admin_menu_items_path
