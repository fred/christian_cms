%h3 Editando Evento

- form_for(@event, :url => { :action => "update" } ) do |f| 
  = f.error_messages
  = render :partial => 'form', :locals => { :f => f, :event => @event }
  = f.submit 'Salvar'
  
= link_to 'Volver', admin_events_path
