%h3
  Comments
- @comments.each do |t|
  .comment
    %h4
      = boolean_to_image_small(t.approved) 
    %p
      name: 
      = h t.name
    %p 
      email:
      = h t.email
    %p
      website:
      = h t.website_url
    %p
      articulo:
      = link_to(t.article.title, admin_article_path(t.article))
    %p
      Submitted on: 
      = t.created_at
    %p
      = h t.comment
    %p
      = approve_link(t)
      |
      = link_to 'Leer', admin_comment_path(t)
      |
      = link_to 'Editar', edit_admin_comment_path(t)
      |
      = link_to 'Delete', admin_comment_path(t), :confirm => 'Estas Seguro?', :method => :delete
