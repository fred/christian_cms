- @title = "Editar Cumpleaño"
%h3= @title

.article

  - form_for(:birthday, :url => admin_birthday_path(@birthday), :html => {:method => :put}) do |f|
    = render :partial => "form", :locals => { :f => f, :birthday => @birthday }
    = f.submit "Salvar" 
